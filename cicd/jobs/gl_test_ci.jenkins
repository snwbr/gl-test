#!/usr/bin/env groovy

multibranchPipelineJob('CI-GL-Test-K8s') {
  displayName('GL-Test repo - K8s validation')
  description('GL-Test repo - K8s manifests validation')
  branchSources {
    git {
      id('120190') // IMPORTANT: use a constant and unique identifier
      remote('git@github.com:snwbr/gl-test.git')
      credentialsId('git-gl-test-ssh')
      includes('*')
    }
  }
  orphanedItemStrategy {
    discardOldItems {
      numToKeep(5)
      daysToKeep(5)
    }
  }
  factory {
    workflowBranchProjectFactory {
      scriptPath('k8s/CI.Jenkinsfile')
    }
  }
}
